name: Update download badge

on:
  schedule:
    - cron: "23 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Count downloads
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          downloads="$(
            gh api --paginate "repos/$GITHUB_REPOSITORY/releases" \
              --jq '[.[] | .assets[] | select(.name | endswith(".dmg")) | .download_count] | add // 0'
          )"

          jq --arg downloads "$downloads" \
            '.message = $downloads' \
            docs/downloads.json > docs/downloads.json.tmp
          mv docs/downloads.json.tmp docs/downloads.json

      - name: Commit badge data
        run: |
          if git diff --quiet; then
            exit 0
          fi

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/downloads.json
          git commit -m "Update download badge"
          git push
